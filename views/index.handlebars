<div class="container py-5">
  <div class="row">
    <div class="col-12 col-lg-10 col-xl-8 mx-auto">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Account Login <i class="fas fa-key"></i></h5>
          <hr>
          <form method="POST" action="/">
            <div class="form-group">
              <label for="InputEmail1">使用者名稱</label>
              <input type="email" class="form-control" id="InputEmail1" aria-describedby="emailHelp"
                placeholder="user@mail.com" name="email" value="{{inputMemberData.email}}">
              <small id="emailHelp" class="form-text text-muted">請輸入註冊的email</small>
            </div>
            <div class="form-group">
              <label for="InputPassword1">使用者密碼</label>
              <input type="password" class="form-control" id="InputPassword1" placeholder="Password" name="password"
                value="{{inputMemberData.password}}">
            </div>

            {{#if error}}
            <div class="alert alert-info" role="alert">
              <span class="mr-2"><code>{{error}}</code></span>
            </div>
            {{/if}}

            <button type="submit" class="btn btn-primary">Sign In</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>